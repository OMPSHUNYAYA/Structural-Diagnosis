## **EVIDENCE POLICY — OPTIONAL MATERIAL**

This case may include an `evidence_optional/` directory.

That directory contains **non-canonical artifacts** generated during
deterministic experimentation, including:
- intermediate runs
- exploratory traces
- comparison or validation outputs

These files are **NOT required** to reproduce or verify the SSD conclusions.

---

## **Authoritative Artifacts**

The **authoritative, citation-ready artifacts** for this case are:
- SSD diagnostic reports (`ssd_report.json` / `.txt`)
- the canonical **SVG evidence snapshot** located in the `ssd_out_*` directory

These artifacts are generated deterministically and are treated as final evidence.

---

## **Reference Visualizations (Non-Evidence)**

Matplotlib PNG plots (if present):
- are **included for inspection and presentation convenience**
- are rendered **only from existing SSD outputs**
- do **not** recompute, reinterpret, normalize, or alter any results
- are **not treated as evidence**

---

## **Citation Rule**

The **SVG evidence snapshot** is the **sole canonical evidence artifact**  
for this case.

All other materials are **supporting, exploratory, or reference-only**.
